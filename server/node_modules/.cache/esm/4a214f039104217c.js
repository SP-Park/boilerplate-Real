let express,mongoose,userApis;_910‍.w("express",[["default",["express"],function(v){express=v}]]);_910‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_910‍.w("./routes/users.js",[["default",["userApis"],function(v){userApis=v}]]);

const path = require("path");
const cors = require('cors');
const cookieParser = require("cookie-parser");

//Router imports


mongoose.connect('mongodb://localhost:27017/KB', {
    useNewUrlParser: true,
    useCreateIndex: true,
    useUnifiedTopology: true,
    useFindAndModify: false
});

const db = mongoose.connection;
db.on("error", _910‍.g.console.error.bind(_910‍.g.console, "connection error:"));
db.once("open", () => {
    console.log("Database connected");
});

const app = express();

app.use(cors());
app.use(cookieParser());
app.use(express.urlencoded({ extended: true }));
app.use(express.json());

app.use('/api/users', userApis);


const port = 5000

app.listen(port, () => {
    console.log(`Server Listening on ${port}`)
  });