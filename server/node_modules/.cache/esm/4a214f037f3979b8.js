let express,mongoose,userApis;_7f3‍.w("express",[["default",["express"],function(v){express=v}]]);_7f3‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_7f3‍.w("./routes/users.js",[["default",["userApis"],function(v){userApis=v}]]);

const path = require("path");
const cors = require('cors');
const cookieParser = require("cookie-parser");

//Router imports


mongoose.connect('mongodb://localhost:27017/KB', {
    useNewUrlParser: true,
    useCreateIndex: true,
    useUnifiedTopology: true,
    useFindAndModify: false
});

const db = mongoose.connection;
db.on("error", _7f3‍.g.console.error.bind(_7f3‍.g.console, "connection error:"));
db.once("open", () => {
    console.log("Database connected");
});

const app = express();

app.use(cors());
app.use(cookieParser());
app.use(express.urlencoded({ extended: true }));
app.use(express.json());







app.use('/api/users', userApis);

app.use('/uploads', express.static('uploads'));

const port = 5000

app.listen(port, () => {
    console.log(`Server Listening on ${port}`)
  });